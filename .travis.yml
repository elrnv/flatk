language: rust

rust:
  - stable
 
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

cache: cargo

script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - cargo +nightly build --all-features --verbose --all
  - cargo +nightly test --all-features --verbose --all
